--Basic select statement

SELECT * 
FROM dbo.customers;

SELECT *
FROM dbo.employees;

SELECT *
FROM dbo.orders;

SELECT first_name,country
FROM dbo.customers;

SELECT emp_id, first_name, salary
FROM dbo.employees